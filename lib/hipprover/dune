(library
 (name hipprover)
 (modules ProversEx entail forward_rules infer_types normalize rewriting heap syntax biab)
 (libraries debug search hipcore provers utils)
 (inline_tests)
 (preprocess (pps ppx_expect)))

(env (dev (flags (:standard -warn-error -A))))
